#!/usr/bin/env python3
#
# p is short for 'prepend'
# or in other words, prepend my clipboard
# with 'duck', so I can use alfred
# (my replacement for spotlight)
# to search duckduckgo using ⌘Space

import pyperclip

clipboard = pyperclip.paste()
new_clipboard = "duck {}".format(clipboard)
pyperclip.copy(new_clipboard)
