#!/usr/bin/env python3
#
# Prints the date in the US format, because I'm used to the European

"""Usage: usdate [--dash]"""

import subprocess
import docopt

args = docopt.docopt(__doc__)

if args["--dash"]:
    subprocess.run("date +%m-%d-%y", shell=True)
else:
    subprocess.run("date +%m/%d/%y", shell=True)
