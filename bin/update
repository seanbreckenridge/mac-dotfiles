#!/usr/bin/env bash
#
# updates brew, python, and anything from the mac app store

# updating brew
bash -x "${DOTDIR}/homebrew/install.sh"

bash -x "${DOTDIR}/python3/install.sh" 2>&1

if [[ $(uname -s) == "Darwin" ]]; then
  "${DOTDIR}/macos/update.sh" 2>&1
fi
